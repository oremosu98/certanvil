# Sec+ curated questions — "Encrypting Data" lesson (Messer SY0-701 §1.4)

**Status:** Fable-authored draft, ready for Sonnet execution.
**Target file:** `certs/secplus.js` → `questionExemplars` array.
**Gap being filled:** all 6 existing obj-1.4 exemplars are PKI/certificates; nothing covers encryption levels, algorithms, key length, or key stretching. One question is deliberately tagged Data Protection / 3.3 (data states are a 3.3 objective, not 1.4).

**Executor checklist:**
1. Append the 10 objects below to `questionExemplars` in `certs/secplus.js` (keep JSON-per-line style of the file).
2. Verify `_formatExemplarsForPrompt` handles them (Q9–Q10 are multi-select with `answers` plural).
3. Run `node tests/uat.js` (exemplar-shape checks live there).
4. Fast lane — cert-pack content only.

```json
{"type":"mcq","question":"A hospital encrypts its patient database files, uses TLS for its web portal, and is now evaluating secure enclaves to protect records while they are actively being processed in memory. Which data state does the secure enclave protect?","difficulty":"Exam Level","topic":"Data Protection","objective":"3.3","options":{"A":"Data at rest","B":"Data in transit","C":"Data in use","D":"Data in escrow"},"answer":"C","explanation":"Data actively being processed in memory is data IN USE — the third and hardest-to-protect state, addressed by access controls, memory protection, and secure enclaves. Database files on disk (A) are data at rest, protected here by database encryption. TLS traffic (B) is data in transit. 'Data in escrow' (D) is not a data state — escrow refers to third-party key custody. Memory trick: Rest, Route, Running — rest = stored, route = moving, running = processing.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"mcq","question":"A sales laptop protected with BitLocker full-disk encryption is stolen from a car. The thief removes the SSD and connects it to another computer to read it. Separately, the security team asks what FDE would NOT have prevented if the laptop had instead been infected with malware while the user was logged in. Which statement is correct?","difficulty":"Exam Level","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"The stolen SSD is readable in the second computer, and FDE would have blocked the malware","B":"The stolen SSD is unreadable without the key, but FDE would not have stopped the malware from reading files through the logged-in session","C":"Both the stolen SSD and the malware scenario are fully mitigated by FDE","D":"FDE protects neither scenario; only file-level encryption helps here"},"answer":"B","explanation":"FDE's core win is the offline-attack scenario: a removed drive is ciphertext without the unlock secret (password/PIN/TPM/recovery key). Its core limitation is that once the authorized user has unlocked the disk, everything is transparently decrypted for that session — malware running under the logged-in account reads files exactly as the user can. This is the classic Security+ FDE trap: strong against lost/stolen devices and offline attacks, weak after login.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"mcq","question":"A Windows admin uses EFS to encrypt a folder of HR documents rather than deploying BitLocker. A security review warns that sensitive content may still leak in cleartext outside the encrypted folder. Which artifact is a legitimate example of this risk?","difficulty":"Hard","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"The EFS-encrypted files themselves, which use a weaker algorithm than BitLocker","B":"The Windows pagefile and application temp files, which can hold unencrypted copies of document content","C":"The NTFS master boot record, which stores a plaintext index of file contents","D":"The TPM, which caches decrypted file contents between reboots"},"answer":"B","explanation":"File-level encryption (EFS) protects only what you point it at. Content routinely escapes into the pagefile/swap, temp files, application caches, and editor autosave copies — all outside the encrypted folder and all cleartext. File names and metadata can also remain visible. That's why organizations often layer file-level encryption ON TOP of full-disk encryption. A is wrong (algorithm strength isn't the issue), C misdescribes the MBR, and D misdescribes the TPM — it protects keys, it doesn't cache file plaintext.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"mcq","question":"A DBA enables Transparent Data Encryption (TDE) on a SQL database holding customer SSNs. An auditor later demonstrates that any user with sufficient database SELECT permissions can still read the SSNs in cleartext. Why is this expected behavior rather than a TDE failure?","difficulty":"Exam Level","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"TDE only encrypts transaction logs, never the data files themselves","B":"TDE protects the stored database files, pages, and backups; once the database engine decrypts data for an authorized session, database permissions govern who sees it","C":"TDE requires each application to encrypt values before insert, and the application skipped that step","D":"TDE keys expire after 90 days, after which data reverts to plaintext"},"answer":"B","explanation":"TDE is storage-layer encryption: it protects database files, storage pages, transaction logs, and backups against theft or direct file copy — transparently, with no application changes. But the database process decrypts data to serve queries, so a user with sufficient DB privileges sees cleartext. Protecting specific fields from privileged users is the job of column-level encryption. Memory trick: 'Whole or Hole' — TDE protects the database as a WHOLE; column-level protects a specific sensitive HOLE (field).","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"mcq","question":"An architect proposes encrypting every individual row and column in a 40-million-record database 'for maximum security.' The DBA pushes back, recommending column-level encryption for the SSN and payment-card columns only. Which principle BEST supports the DBA's position?","difficulty":"Hard","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"Encrypting all fields is impossible because databases support at most one encrypted column per table","B":"Data classification and risk-based control selection — per-field encryption adds CPU, key-management, indexing, and query overhead, so it should be spent on the fields that need it","C":"Security through obscurity — fewer encrypted columns make it harder to guess which data matters","D":"Non-repudiation — encrypting fewer columns produces stronger audit evidence"},"answer":"B","explanation":"Encrypting every field individually multiplies CPU cost, key operations, and application logic, and degrades searching, sorting, indexing, and reporting. The logical approach is to CLASSIFY the data and apply stronger (column-level) encryption to the genuinely sensitive fields — SSNs, card numbers, medical data, salaries — while non-sensitive fields (like an employee ID) stay readable. This is data classification + risk-based security + efficient control use. A is fabricated, C misuses 'security through obscurity', and D has nothing to do with non-repudiation.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"mcq","question":"During a compliance review, a legacy application is found encrypting archived records with DES. The vendor argues the algorithm is 'battle-tested for decades.' What is the correct Security+ assessment?","difficulty":"Exam Level","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"DES is acceptable because algorithm age proves reliability","B":"DES is obsolete — its 56-bit effective key length is brute-forceable — and should be replaced with AES (128/192/256-bit keys)","C":"DES is fine as long as the algorithm itself is kept secret from attackers","D":"DES should be upgraded to 3DES, which is the current recommended standard"},"answer":"B","explanation":"DES's 56-bit effective key is trivially brute-forceable with modern hardware — it is obsolete and insecure. AES is the modern symmetric standard (128-, 192-, or 256-bit keys). C is a double error: a secure cryptosystem must NOT depend on hiding the algorithm — the algorithm can be public; only the KEY must be protected (Kerckhoffs's principle). D is wrong for 'current recommended' — 3DES is legacy, slow, and being retired; it's a stopgap, not a destination. Exam lock-in: AES = modern symmetric, DES = never pick it.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"mcq","question":"A junior engineer claims a 2048-bit RSA key is 'eight times stronger' than a 256-bit AES key because 2048 is a bigger number. What is the correct response?","difficulty":"Hard","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"Correct — key strength scales linearly with bit count across all algorithms","B":"Incorrect — symmetric and asymmetric key lengths cannot be compared directly because the algorithms rely on different mathematics","C":"Incorrect — RSA-2048 is actually weaker than ANY symmetric key because asymmetric encryption is inherently insecure","D":"Correct — but only when both keys are stored in an HSM"},"answer":"B","explanation":"This is a flagged exam trap. Within the SAME algorithm family, longer keys mean more brute-force resistance (each added bit doubles the keyspace: 2^128 vs 2^256). But ACROSS families the numbers aren't comparable: RSA keys must be much larger (2048/3072/4096 bits) because RSA's security rests on integer factorization, which has far better attacks than brute-forcing a random symmetric key. AES-128 is already extremely brute-force resistant. C overcorrects into nonsense, and D is irrelevant — an HSM protects keys, it doesn't change algorithm math.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"mcq","question":"A web application stores password-derived values using PBKDF2 with 600,000 iterations and a unique salt per user. An attacker who can normally test one billion fast hashes per second steals the database. What has the iteration count primarily achieved?","difficulty":"Exam Level","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"It converted each password into a genuinely random 256-bit key, making guessing impossible","B":"It made each individual password guess computationally expensive, collapsing the attacker's guess rate while adding only a tiny delay for legitimate logins","C":"It encrypted each password 600,000 times so the attacker must decrypt 600,000 layers","D":"It lengthened each password by 600,000 characters"},"answer":"B","explanation":"This is key stretching: a key derivation function (PBKDF2, bcrypt, scrypt, Argon2) deliberately burns CPU (and, for scrypt/Argon2, memory) on every derivation. A legitimate user pays the cost once per login — milliseconds. An attacker making billions of guesses pays it billions of times, collapsing their effective rate. Two flagged traps: key stretching does NOT mean encrypting the key repeatedly (C) and does NOT physically lengthen the password (D) — 'stretching adds time, not characters.' A weak password is still weak (A), which is why stretching is combined with salting, long passphrases, MFA, and rate limiting.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"multi-select","question":"(Choose TWO) A company deploys full-disk encryption on all laptops and TLS on all web traffic, then declares its data 'fully protected by encryption.' Which statements correctly identify what this architecture does NOT cover?","difficulty":"Hard","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"An authorized logged-in user copying sensitive files to a USB drive — the disk is already unlocked for their session","B":"Data being actively processed in application memory, which neither FDE nor TLS addresses","C":"A stolen powered-off laptop, because FDE only works while the OS is running","D":"HTTPS sessions to the company web app, because TLS cannot protect web traffic","E":"Offline attacks against a removed hard drive, which FDE does not resist"},"answers":["A","B"],"explanation":"A and B are the real gaps. Encryption at rest doesn't stop an AUTHORIZED user (or malware in their session) from exfiltrating data once the disk is unlocked — that needs DLP, monitoring, and least privilege. And data in use (in memory) is a third state that needs its own controls (secure enclaves, memory protection, access control). C, D, and E each invert what FDE/TLS are BEST at: FDE's strongest cases are exactly the powered-off stolen laptop and the removed-drive offline attack, and protecting HTTPS web traffic is precisely what TLS does. Broader lesson: encryption provides confidentiality — it is not the whole CIA triad, and no single encryption layer covers every location or threat.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},

{"type":"multi-select","question":"(Choose TWO) A remote employee's browser already uses HTTPS for every site. The security team still requires a TLS-based remote-access VPN before reaching internal resources. Which statements accurately describe what the VPN adds beyond per-site HTTPS?","difficulty":"Hard","topic":"Cryptography Fundamentals","objective":"1.4","options":{"A":"The VPN can encrypt traffic from ALL applications routed through the tunnel, not just the browser's TLS-capable traffic","B":"The VPN creates an encrypted tunnel across the untrusted network into the protected environment, gating access to internal-only resources","C":"The VPN re-encrypts each HTTPS session with a second TLS layer, doubling the cryptographic strength of the same traffic","D":"The VPN replaces the need for TLS on internal web applications permanently","E":"HTTPS and VPNs use incompatible encryption, so the browser must disable TLS inside the tunnel"},"answers":["A","B"],"explanation":"A and B capture the application-vs-network distinction: a browser's TLS protects that application's traffic only, while a VPN encrypts whatever traffic is routed through the tunnel — legacy apps, file shares, DNS — regardless of which application generated it, and it provides the encrypted path into the company network (client/remote-access VPNs are commonly TLS-based; site-to-site VPNs commonly use IPsec at the network layer). C is a distractor — tunneled HTTPS is indeed doubly wrapped, but 'doubling cryptographic strength' is not why VPNs are deployed and isn't a Security+ rationale. D is backwards (defense in depth keeps internal TLS), and E is fabricated.","source":"curated-lesson-encrypting-data","addedVersion":"7.80.0","addedDate":"2026-07-20"},
```

## Coverage map (lesson section → question)

| Lesson section | Q | Topic / objective |
|---|---|---|
| Three data states | Q1 | Data Protection / 3.3 |
| FDE strengths + post-login limitation | Q2 | Cryptography Fundamentals / 1.4 |
| File-level (EFS) leakage artifacts | Q3 | Cryptography Fundamentals / 1.4 |
| TDE vs privileged users ("Whole or Hole") | Q4 | Cryptography Fundamentals / 1.4 |
| Column-level + data classification | Q5 | Cryptography Fundamentals / 1.4 |
| DES/3DES/AES + Kerckhoffs | Q6 | Cryptography Fundamentals / 1.4 |
| RSA-vs-AES key-length trap | Q7 | Cryptography Fundamentals / 1.4 |
| Key stretching / KDFs | Q8 | Cryptography Fundamentals / 1.4 |
| Encryption ≠ full CIA / data-in-use gap | Q9 | Cryptography Fundamentals / 1.4 |
| Transport: app TLS vs VPN scope | Q10 | Cryptography Fundamentals / 1.4 |

Not covered (already exam-locked elsewhere or thin in the notes): HSM as a dedicated question (mentioned inside Q7 distractor; consider a future HSM/TPM/secure-enclave comparison question), hashing-vs-encryption (notes flag it, but it overlaps the existing hashing coverage — check before authoring).
